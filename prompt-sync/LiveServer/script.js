// Pede o nome do usuario usando uma caixa de prompt
// o que o usuario digitar será salvo na variavel "nome"
let nome = prompt("Qual seu nome" )

// Cria uma mensagem de boas-vindas
let mensagem = "Olá" + nome + "Seja bem-vindo (a)"

// Mostra a mensagem na tela usando uma caixinha de alerta
aler(mensagem)